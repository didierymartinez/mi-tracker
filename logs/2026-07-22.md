# 📆 Martes 22 de julio de 2026

**Foco del día:** Event Sourcing (Dometrain) — sección de código: command handlers.

---

## 🔁 Loops de estudio

### Loop 1 ✅ — Dometrain: "Building Command Handlers" (cap. 12) — visto y consolidado
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 12. La lección más larga/compleja del capítulo.
- **`StoredEvent`** (record): metadatos de persistencia — `AggregateId`, `SequenceNumber`, `Timestamp` (DateTime UTC), `object EventData`. Dentro del aggregate el evento es solo `object`; al persistir necesita saber a qué aggregate, orden y cuándo.
- **`IEventStore`** (interfaz): `GetEvents(aggregateId)` · `AppendEvent(storedEvent)` (prefijo `app`, `event` es reservada) · `SaveChanges()` como **unit of work**.
- **`EventStream<TEntity>`** (`where TEntity : AggregateRoot, new()`): `GetEntity()` = fetch todos los eventos → `new TEntity()` → `foreach` aplicando `entity.Apply((dynamic)e.EventData)` (el cast a dynamic elige el overload correcto; si no, iría al `Apply(object)` vacío) + guarda `lastSequenceNumber`. `Append(obj)` = `++seq`, crea `StoredEvent(id, seq, UtcNow, data)`, `store.AppendEvent`.
- **`CommandHandler<TCommand>`** (abstract, primary ctor con `IEventStore`): `abstract Handle(TCommand)` + `protected GetStream<TEntity>(Guid id)`. NO mapear genéricamente command→entidad (fricción); cada handler pide su stream.
- **Ejemplo `AddShippingLabelHandler`:** `GetStream<Box>(command.BoxId).GetEntity()` (dispara replay) → `if (command.Label.IsValid())` append `ShippingLabelAdded` else append `ShippingLabelFailedToAdd(FailReason.TrackingCodeInvalid)`. Los value types brillan aquí.
- **Clave:** el handler **NO guarda** — `SaveChanges()` se hace en el **command router**, para poder **encadenar handlers en una sola transacción**.
- **Validación (3/3):** (1) `SaveChanges()` fuera del handler → el **router encadena varios handlers en una sola transacción** (atómica); (2) el `(dynamic)` resuelve el overload de `Apply` en runtime según el tipo real, sin él enlaza al `Apply(object)` vacío y no reconstruye estado (= su bug del workshop, del otro lado); (3) `SequenceNumber` fija el orden de replay, y se lleva `lastSequenceNumber` para asignar `last+1` al append (base también de concurrencia optimista).
- **Payload NotebookLM:** `.sync/notebooklm_outbox/2026-07-22_building-command-handlers.md`.

### Loop 2 ✅ — Dometrain: "Adding a Command Router" (cap. 13) — visto y consolidado
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 13.
- **Por qué el router:** `SaveChanges` no va en el handler para permitir **composabilidad** (descomponer un command complejo en varios pequeños, ejecutar sus handlers, y un solo `SaveChanges` al final). El **router** orquesta: localiza el handler → lo ejecuta → `SaveChanges()`.
- **Scope:** el router necesita el `IEventStore` del **mismo scope** para que `SaveChanges` opere sobre la **misma instancia** que usó el handler.
- **Enfoque 1 (switch):** `switch(command)` con un `case` por tipo, `new XHandler(eventStore).Handle(cmd)`, `default` → throw. Funciona pero la clase crece y se ensucia si un handler necesita **deps extra** (habría que inyectarlas también en el router).
- **Enfoque 2 (reflection + DI, el de Boyne):** inyecta `IServiceProvider` (mismo scope → misma instancia de store). ~5 líneas de reflexión: tipo del command → construir `CommandHandler<T>` → `GetService` (handler con sus deps resueltas por DI) → `MethodInfo` de `Handle` → invoke → `SaveChanges()`.
- **Resultado:** el command router es el **único punto de interacción con el dominio** (se enchufa a APIs). Registrar store + handlers → lanzarle commands.
- **Validación (1.5/3):** P2 ✅ (SaveChanges fuera del handler = unit of work + composabilidad → encadenar handlers, un solo save). **P1 ⚠️ corregida:** el store scoped acumula appends en memoria; si el router hace `SaveChanges` sobre **otra instancia** distinta a la del handler, esa instancia no tiene los eventos → **no persiste nada (se pierden)** — NO es tema de orden. Mismo scope = misma instancia. **P3 ⚠️ matizada:** el problema real que resuelve reflection+DI no es "un case por comando" (secundario) sino la **inyección de dependencias** de cada handler (con switch habría que inyectar las deps extra también en el router → constructor mess).
- **Payload NotebookLM:** `.sync/notebooklm_outbox/2026-07-22_adding-a-command-router.md`.

### Loop 3 📌 — Dometrain: "An exercise for you to make" (cap. 14) — EJERCICIO práctico (por implementar)
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 14. No es concepto nuevo: es **código para escribir**.
- **Tarea:** implementar los **4 command handlers restantes** del aggregate `Box` (solo está `AddShippingLabel`), añadir sus `Apply(...)` en la root entity, y reorganizar `Box.cs` en archivos por concepto.
- **Spec del dominio:**
  - **Create box** → `BoxCreated` (tamaño vía `BoxCapacity`). Nunca falla.
  - **Add shipping label** → `ShippingLabelAdded` / `ShippingLabelFailedToAdd` (fallo como **evento**).
  - **Add beer bottle** → `BeerAdded` / box llena → **excepción**.
  - **Close box** → `BoxClosed` / box vacía → **excepción**.
  - **Ship box** → `BoxShipped` (si cerrada **y** con label) / **dos razones** de fallo.
- **Observación de diseño:** el curso **mezcla fallo-como-evento vs fallo-como-excepción** — decidir según si el fallo es parte del relato del negocio (evento, auditable/reproducible) o solo una guardia técnica (excepción).
- **Estado:** 📌 pendiente de implementar (ideal en el workshop propio). Payload NotebookLM: `.sync/notebooklm_outbox/2026-07-22_exercise-box-domain.md`.

### Loop 4 ✅ — Dometrain: "Introduction to the test base class" (cap. 15) — visto y consolidado
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 15. *(Toca deuda técnica 🔴 C#/testing.)*
- **`TestStore`:** `IEventStore` **in-memory sin mocks**. `PreviousEvents` (pasado, se llena por test) y `NewEvents` (lo que emite el handler). `GetEvents` lee de PreviousEvents (rehidrata el aggregate); `AppendEvent` escribe en NewEvents → inspeccionas storage directo.
- **Base test Given/When/Then:**
  - **Given** = eventos pasados → `PreviousEvents` (objetos → StoredEvent). Overload con **aggregate ID ambiente** (99% tests de un solo aggregate → más limpio).
  - **When** = llama `Handle`; el **handler es propiedad abstracta** que cada test implementa (no sabemos sus deps extra). El TestStore lo alimenta.
  - **Then** = de `NewEvents`, ordena y `Select` del `EventData`; (1) chequea el **número** de eventos; (2) en paralelo, **tipos iguales** + **`BeEquivalentTo`** (compara propiedades a fondo → no requiere `Equals` en los eventos).
- **Caveat FluentAssertions:** dos eventos **vacíos** (sin props) → `InvalidOperationException` "no members were found for comparison"; como ya se validó el tipo, se **traga** esa excepción concreta y se relanza cualquier otra.
- **Extra:** soporta **múltiples eventos** por command. Recomienda **FluentAssertions**.
- **Validación (correspondencia G/W/T ↔ ES):** **Given** = eventos pasados (`PreviousEvents`) que por **replay** producen el estado (⚠️ matiz: son *eventos*, no un setter de estado); **When** = ejecutar `Handle` (⚠️ el handler **appendea** a `NewEvents`, no "retorna" el evento); **Then** = comparar eventos nuevos vs esperados ✅. Insight: el handler ≈ función `(eventos pasados, command) → eventos nuevos` → testeable sin mocks ni DB (enlaza con aislamiento/sin efectos de cap. 6).
- **Payload NotebookLM:** `.sync/notebooklm_outbox/2026-07-22_intro-test-base-class.md`.

### Loop 5 ✅ — Dometrain: "Writing event sourced tests" (cap. 16) — visto y consolidado
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 16. *(C#/testing aplicado 🔴.)*
- **Organización:** tests agrupados como los aggregates (carpeta por aggregate).
- **Primer test (`AddBeerHandlerTest`):** hereda la base test e implementa la propiedad abstracta `Handler => new AddBeerBottleHandler(EventStore)`. De ahí, unit tests normales.
- **Given/When/Then (xUnit `[Fact]`):** `Given(new BoxCreated(new BoxCapacity(6)))` → `When(new AddBeer(BoxId ambiente, cerveza))` → `Then(new BeerBottleAdded(...))`.
- **Detalle clave:** en el esperado crea una **instancia NUEVA** de la cerveza (no reusa la referencia del command) → prueba que la comparación es por **contenido (`BeEquivalentTo`, deep)** y **no por referencia**.
- **Anti-trampa:** cambia algo profundo del object graph (un "." en el nombre) → el test **falla** → confirma que sí asserta el contenido.
- **Patrón universal:** eventos pasados → encolar command → esperar eventos nuevos (múltiples permitidos en given y expected).
- **Validación:** P1 ✅ instancia nueva → comparación por **valor** (`BeEquivalentTo`), evita depender de la igualdad por referencia. P2: Given = `BoxCreated(BoxCapacity(6))` (caja vacía) · When = `AddBeer(BoxId, cerveza)` · Then = `BeerBottleAdded(cerveza)`.
- **Payload NotebookLM:** `.sync/notebooklm_outbox/2026-07-22_writing-event-sourced-tests.md`.

### Loop 6 ✅ — Dometrain: "Making our tests more readable" (cap. 17) — visto y consolidado
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 17. *(C#/testing 🔴.)*
- **Meta:** tests legibles hasta para un product owner. Refactor del mismo test a un mini-DSL.
- **Base class intermedia por aggregate (`BoxTest`):** entre `CommandHandlerTest<T>` y el test concreto; genérica en `TCommand` sin constraints. Alias `protected Guid BoxId => AggregateId;`.
- **Builders de eventos** (en la base compartida, se reutilizan): `BoxCreatedWithCapacity(6)`, `BeerBottleAdded(bottle)`.
- **Builder del command** al **fondo del test concreto** (solo se usa ahí): `AsBeerBottle(bottle) => new AddBeerBottle(BoxId, bottle)`.
- **Test data** con nombre en la base compartida: `CartBlanche` (la cerveza).
- **Resultado:** `Given(BoxCreatedWithCapacity(6)); When(AsBeerBottle(CartBlanche)); Then(BeerBottleAdded(CartBlanche));` → casi lenguaje natural. Sin SpecFlow/Cucumber/mocking.
- **Regla de ubicación:** eventos + test data compartidos en la base del aggregate (reusados por varios handlers); command builder en el test concreto (solo se usa ahí).
- **Validación (2/2):** P1 ✅ criterio = **reutilización** (eventos + test data compartidos → base; command builder → test concreto). P2 ✅ legible para funcionales/PO + **documentación viva del dominio** (caza malentendidos temprano) + mantenibilidad/predictibilidad + sin SpecFlow/Cucumber/mocking.
- **Payload NotebookLM:** `.sync/notebooklm_outbox/2026-07-22_making-tests-readable.md`.

### Loop 7 📌 — Dometrain: "Another exercise" (cap. 18) — EJERCICIO (cierre del capítulo de tests)
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 18. No es concepto: **ejercicio de tests**.
- **Tarea:** separar en dos archivos y escribir **más tests para el aggregate `Box`** — cubrir todos los escenarios: disparar cada **evento de fallo** y también los **eventos de éxito** en distintos casos.
- Boyne provee un **zip con todos los tests** al final del capítulo para comparar.
- **Estado:** 📌 pendiente de implementar. Encaja perfecto con el ejercicio de la cap. 14 → hacer los 4 handlers **con** sus tests (TDD-ish). *(No genera payload NotebookLM: es tarea, no contenido nuevo.)*

### Loop 8 ✅ — Dometrain: Ports & Adapters — intro command side (cap. 19) — visto y consolidado
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 19. Intro del capítulo (título genérico "Event Sourcing in .NET"). *(Arquitectura 🟡.)*
- **Lo construido:** un dominio event-sourced (todos los commands + events del event storm, verificado con tests), **agnóstico** a la app → Clean Architecture / **Ports & Adapters (hexagonal)**.
- **Dos puertos:** **driving port** (entrada) = el **command router** (acepta commands); **driven port** (salida) = la interfaz **`IEventStore`** (qué espera el dominio del store).
- **Roadmap del capítulo:** (1) implementar `IEventStore` contra **SQL Server** (una tabla); (2) poner una **API** al frente que consume el command router; (3) la API es la startup app → **registra el event store en DI**.
- **Resultado:** command side completo de CQRS corriendo; falta el **read side (projections)**.
- **Conexión clave:** `TestStore` (in-memory) y el **SQL event store** son **dos adapters del mismo driven port** → por eso los tests fueron tan limpios (inversión de dependencias, se cambia el adapter sin tocar el dominio).
- **Validación (1.5/2):** P1 ✅ el adapter ya escrito = **TestStore**; misma interfaz → intercambiar adapters sin tocar el dominio (inversión de dependencias, tests limpios). **P2 ⚠️:** driven port = `IEventStore` ✅, pero el **driving port es el command router, NO la API** (la API es un *driving adapter* que entra por ese puerto). Dirección: al dominio lo conducen por el driving port (mundo→dominio); el dominio conduce la infra por el driven port (dominio→infra).
- **Payload NotebookLM:** `.sync/notebooklm_outbox/2026-07-22_ports-and-adapters-command-side.md`.

### Loop 9 ✅ — Dometrain: SQL Event Store setup (Dapper) (cap. 20) — visto y consolidado
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 20. Título genérico "Event Sourcing in .NET".
- **Web/API:** ASP.NET (Razor + controllers) + **OpenAPI/Swagger**, controller placeholder, connection strings LocalDB en appsettings.
- **Event store (stub):** implementará `IEventStore` con **Dapper** (ejecutar SQL sin ceremonia). **Connection factory** que saca la cadena de `IConfiguration` y crea `SqlConnection`.
- **`DatabaseEvent`** (repr. en BD ≠ `StoredEvent` en dominio): `AggregateId`, `SequenceNumber`, `Timestamp`, y en vez de `EventData` → **`EventTypeName`** + **`EventBody` (JSON)**. Extra: columna SQL **`timestamp`/rowversion** (auto-incrementa al tocar el registro → útil para **projections**).
- **Mapping bidireccional:** StoredEvent→DatabaseEvent (type name + serializar JSON); DatabaseEvent→StoredEvent (type name→Type + deserializar).
- **CREATE TABLE:** `nvarchar(max)` body, `nvarchar(256)` type name, **PK compuesta (AggregateId, SequenceNumber)** → **concurrency checking automático** (no se repite seq para el mismo aggregate) + writes en páginas distintas (más concurrencia, a costa de page splits).
- **Validación (2/2):** P1 ✅ el 2º INSERT con la misma `(AggregateId, SequenceNumber)` viola la PK → **error de clave duplicada** → solo un write gana, el otro reintenta (replay + re-decidir) = **concurrencia optimista sin locks**. P2 ✅ `EventTypeName` = a qué tipo deserializar (→ Apply correcto); `EventBody` (JSON) = el payload/estado; se usa JSON porque la BD no persiste un `object` de C#.
- **Payload NotebookLM:** `.sync/notebooklm_outbox/2026-07-22_sql-event-store-setup.md`.

### Loop 10 ✅ — Dometrain: Implementing the Event Store (Dapper) (cap. 21) — visto y consolidado
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 21. Se reemplazan las 3 `NotImplementedException` con Dapper.
- **GetEvents (SELECT):** query de los 6 campos `WHERE AggregateId=@aggregate ORDER BY SequenceNumber` (orden correcto o rehidrata mal). `using` connection (de la factory, vuelve al pool). `connection.Query<DatabaseEvent>(sql, new { aggregate })` → `.Select(e => e.ToStoredEvent())`.
- **AppendEvent + SaveChanges = unit of work** (estilo EF DbContext): `AppendEvent` solo **cachea** en `List<StoredEvent> newEvents`; `SaveChanges` ejecuta y commitea.
- **SaveChanges (INSERT + transacción):** abrir connection → `using` `BeginTransaction()` → `connection.Execute(insertSql, newEvents.Select(DatabaseEvent.From), transaction)` (Dapper detecta colección → un insert por item) → `transaction.Commit()` → limpiar `newEvents`. Todo se commitea junto o nada (atómico).
- **Nota:** sistemas avanzados agregan metadata (correlation/conversation IDs). Este es el event store más simple que funciona.
- **Validación (2/2):** P1 ✅ AppendEvent cachea → SaveChanges commitea en 1 transacción = unit of work, todos-o-nada. **→ Resuelve el pendiente de cap. 13:** varios handlers acumulan en la misma unidad SOLO si comparten la misma instancia del store → por eso el router los resuelve del mismo scope. P2 ✅ sin `ORDER BY SequenceNumber` el replay va fuera de orden → estado incorrecto (el orden ES el estado).
- **Payload NotebookLM:** `.sync/notebooklm_outbox/2026-07-22_implementing-event-store-dapper.md`.
- **Duda resuelta (`ToStoredEvent` / `FromStoredEvent`):** no son de Dapper — son un simple **map** entre `StoredEvent` (dominio) y `DatabaseEvent` (BD), definidos en la clase `DatabaseEvent` en la cap. 20. `FromStoredEvent` (dominio→BD: type name + serializa JSON) y `ToStoredEvent` (BD→dominio: deserializa JSON según type name). En cap. 21 solo se enchufan al `Query`/`Execute`.

### Loop 11 🟡 — Dometrain: Enchufando el dominio a una API (Controllers) (cap. 22, skimming previo)
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 22 · **En progreso (aún no visto).** No es curso de ASP.NET; solo cómo enchufar el ES a una app.
- **URL:** `api/command/{aggregate}/{command}` (ej. `api/command/box/create`). Controller method `IActionResult CreateBox`, `[Route("create")]` + `[HttpPost]`.
- **Contratos:** con terceros, **mapear externo→interno** (no reutilizar el command). Boyne bindea directo `[FromBody] CreateBox` con comentario de que deberías mapear; en apps internas importa menos.
- **Command router por DI:** se inyecta (del dominio) → `router.Handle(command)`.
- **Respuesta `202 Accepted`** (no 200 OK) a propósito: las projections no corren síncronas → 202 = "recibido bien, procesamiento (read-side) aún no terminado" → consistencia eventual. (Devolver 200 también válido.)
- **Estado:** pendiente de ver; payload NotebookLM en `.sync/notebooklm_outbox/2026-07-22_api-controllers.md`.

---

## ⏭️ Pendiente
- **🧩 EJERCICIO caps. 14 + 18 (combinar):** implementar los 4 command handlers del Box (create / add beer / close / ship) + sus Apply, y escribir sus tests (éxito y todos los fallos) con el patrón Given/When/Then legible. Comparar con el zip de soluciones de Boyne.
- Subir a NotebookLM los payloads del outbox — 17 pendientes.
- Confirmar "ya lo vi" de "API Controllers" para validar.
- **Micro-repaso pendiente:** (1) loose vs strict al deserializar (cap. 10); (2) driving port = command router (la API es el adapter) (cap. 19). *(cap. 13 scope: ✅ RESUELTO en cap. 21. cap. 15: Given = eventos, no estado.)*
