# 📆 Miércoles 15 de julio de 2026

**Foco del día:** Event Sourcing — estudio (Loop) + maduración del workshop propio.

---

## 🔁 Loops de estudio

### Loop 1 ✅ — Dometrain: "Future, Present & Past" (Event Sourcing en .NET)
- **Curso:** Event Sourcing con Marten & .NET (Dometrain).
- **Concepto núcleo:** los tres tiempos — **comando = futuro** (petición no ejecutada), **estado = presente**, **evento = pasado** (ya ocurrió y se comunicó afuera).
- **Por qué guardar eventos y no comandos:**
  1. La **lógica de negocio evoluciona**: reprocesar el mismo comando mañana puede dar un resultado distinto al ya comunicado al exterior.
  2. Reprocesar comandos re-dispara **lógica pesada y efectos secundarios** (APIs, storage); el replay de eventos es liviano (solo re-asigna propiedades).
- **Direccionalidad:** eventos → estado (replay) sí; estado → eventos no. Guardar solo estado = CRUD clásico (se pierde la historia).
- **Validación:** 3/3 conceptos correctos; único ajuste: la razón #1 es *la lógica que cambia*, no *el estado actual*. **Comprensión sólida.**

### Loop 2 ✅ — Dometrain: "The relationship with DDD" (repaso)
- ES ≠ DDD (ES es solo patrón de almacenamiento). Encajan porque el negocio habla en comandos/eventos → **Event Storming** traduce post-its 1-a-1 a clases `Command`/`Event`.
- Eventos inmutables → no editas el pasado, emites **acción compensatoria** (nota de crédito). El estado es un subproducto del stream.

### Loop 3 ✅ — Dometrain: "Events, streams & aggregates" (repaso) → **Sprint completo (3 Loops)**
- Un evento pertenece a **un solo stream** (ligado por id). Estado = replay en memoria; orden importa → **sequence number** (base de concurrencia). Stream **append-only**.
- Terminología DDD: **stream** (eventos persistidos) · **aggregate root** (la entidad, lógica+estado) · **aggregate** (scope de decisión del comando).

> **Sprint del día = 3/3 Loops** en repaso de *Event Sourcing in .NET* (Dometrain). Consistencia > intensidad. ✅

### Loop 4 ✅ — Dometrain: "The command handler life cycle" (cap. 6) — **visto 21/jul y consolidado**
- **Curso:** Event Sourcing con Marten & .NET (Dometrain) · cap. 6 Command Handler.
- **Ciclo de vida de un comando (6 pasos):** (1) comando entra → **handler**; (2) **localizar** el aggregate/stream vía propiedad(es) del comando (normalmente el id); (3) **fetch** de todos los eventos del stream; (4) **replay** para reconstruir estado en memoria (liviano: miles de eventos en ms, sin re-validar el pasado); (5) **execute** → produce **nuevos eventos** (1..N); (6) **append** al stream.
- **Propiedades clave:** manejar un comando **no tiene efectos secundarios** sobre otros aggregates ni otras partes del sistema → se desarrolla en **aislamiento** → esfuerzo ≈ constante por handler → **estimación predecible** (contar comandos del feature). A escala (miles de eventos) → **snapshotting / caching**.
- **Validación (5.5/6):** decisión de negocio vive en el **execute**; si se rechaza y no hay eventos, **no hay append → stream intacto** (correcto). Ajustes: (a) el paso 6 es **append** (persistir), no *apply* — *apply* es la operación interna del **replay**; (b) el beneficio de estimación = un handler nuevo cuesta ≈ lo mismo, así que se estima un feature **contando sus comandos**.

---

## 🛠️ Workshop Event Sourcing (mantenimiento y construcción)

- **Bug real encontrado siguiendo §04** (`RuntimeBinderException: ... inaccessible due to its protection level`): `dynamic` respeta la accesibilidad desde el sitio de la llamada. Como `((dynamic)this).Apply(...)` vive en la clase base `AggregateRoot`, los `Apply` de `Persona` deben ser `public` (no `protected`/`private`). Corregido en §04, §07 y fundamentals §03, con WARNING explicativo. Contraste con Marten (que usa reflexión y sí accede a no-públicos) aclarado en §27.
- **Secciones nuevas (huecos 🧩 del Nivel Básico):** §02b Genéricos y restricciones (`where T`) y §06b SOLID en ejemplos reales.

---

## ⏭️ Pendiente
- `bash sync.sh` (o Claude Code CLI) para versionar workshop + tracker.
- Subir payload de NotebookLM de la lección.
- Siguiente Loop: continuar Dometrain o secciones del workshop.
